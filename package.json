{
	"name": "udaan-scholarship-finder",
	"private": true,
	"scripts": {
		"client": "npm run dev --prefix frontend",
		"server": "npm run dev --prefix backend",
		"dev": "concurrently \"npm run server\" \"npm run client\""
	},
	"devDependencies": {
		"concurrently": "^10.0.3"
	}
}
